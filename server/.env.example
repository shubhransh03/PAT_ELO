# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/therapy_casemgmt

# Clerk Configuration (for token verification - to be implemented)
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Session Configuration
SESSION_REMINDER_THRESHOLD=10

# File Upload Configuration
MAX_FILE_SIZE=10mb
UPLOAD_PATH=./uploads

# Development Helpers
# Set to true to allow the API server to start even if MongoDB is unreachable
# (routes that hit the database will still fail). Useful for frontend-only work.
SKIP_DB=false
