# Environment configuration for PAT_ELO backend
# Copy this file to `.env` and adjust values per environment.
#
# Notes:
# - In development, you can set SKIP_DB=true and/or SKIP_AUTH=true to run without a DB/auth.
# - In production, SKIP_* are automatically disallowed by validation and will throw.
# - Set CORS_ORIGIN to a comma-separated list of allowed front-end origins.
# - Enable API docs by setting ENABLE_API_DOCS=true (recommended only in dev/stage).

# Node environment: development | test | production
NODE_ENV=development

# Server port
PORT=4000

# MongoDB connection string (required in production)
MONGODB_URI=mongodb://localhost:27017/pat_elo

# Clerk secret key for backend JWT validation (required when auth enabled)
CLERK_SECRET_KEY=

# Comma-separated list of allowed origins for CORS
CORS_ORIGIN=http://localhost:5173

# Enable Swagger API docs (default: false)
ENABLE_API_DOCS=false

# Development toggles (ignored in production)
SKIP_DB=false
SKIP_AUTH=false# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/therapy_casemgmt

# Clerk Configuration (for token verification - to be implemented)
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Auth Dev Switches (development only)
# Set to true to bypass token verification for local development
SKIP_AUTH=true

# Session Configuration
SESSION_REMINDER_THRESHOLD=10

# File Upload Configuration
MAX_FILE_SIZE=10mb
UPLOAD_PATH=./uploads

# Development Helpers
# Set to true to allow the API server to start even if MongoDB is unreachable
# (routes that hit the database will still fail). Useful for frontend-only work.
SKIP_DB=false

# CORS configuration (comma-separated list)
CORS_ORIGIN=http://localhost:5173
