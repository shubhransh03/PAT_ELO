/**
 * Archived legacy file (server-test.js)
 * This file previously started an Express server for ad-hoc tests.
 * It is now a no-op to prevent port conflicts during test runs.
 */
export default {};
